<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cloud.api.mapper.BlogThehall.TaskCommentsMapper">
    <resultMap id="Comm" type="com.cloud.api.bean.vo.TaskCommentsVo">
        <id column="comm_id" property="commId"/>
        <result column="comm_content" property="commContent"/>
        <result column="comm_father_id" property="commFatherId"/>
        <result column="comm_time" property="commTime"/>
        <result column="open_id" property="openId"/>
    </resultMap>
    <select id="selectSonCount" resultType="java.lang.Integer">
        select count(*) from task_comments where comm_father_id=#{comm_father_id}
    </select>
    <select id="seelectFcomm_id" resultType="java.lang.String">
        select comm_father_id from task_comments where comm_id=#{comm_id}
    </select>
    <select id="selectSonyByFid" resultMap="Comm">
        select  comm_id,open_id,comm_content,comm_father_id,comm_time
        from task_comments where comm_father_id=#{comm_father_id} and comm_delete=0
    </select>
    <select id="select1FComm" resultType="com.cloud.api.bean.vo.TaskCommentsVo">

    </select>

</mapper>