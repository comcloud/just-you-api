<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cloud.api.mapper.MessageMapper">

    <select id="selectUserFromOpenid" resultType="com.cloud.api.bean.entity.User">
        select user_name,user_head_portrait,email
        from user
        where open_id = #{openid}
    </select>
    <select id="selectTaskFromId" resultType="com.cloud.api.bean.entity.Task">
        select id,task_title
        from task
        where id = #{taskId}
    </select>
    <select id="selectDynamicFromId" resultType="com.cloud.api.bean.entity.Dynamic">
        select dynamic_id , dynamic_title from dynamic where dynamic_id = #{dynamicId}
    </select>
</mapper>